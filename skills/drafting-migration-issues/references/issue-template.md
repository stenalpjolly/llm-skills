# Issue Template

When drafting issues, output them as a JSON array of objects matching this schema. Do not deviate.

```json
[
  {
    "title": "[Frontend] Migrate {FileName} to Angular Material",
    "labels": ["migration", "frontend", "angular-material"],
    "body": "## 1. Objective & Target\n- **Original File:** `{FilePath}`\n- **Target Tech Stack:** Standalone Angular 17+, TypeScript, and Angular Material (`<mat-*>`)\n- **Goal:** Migrate the React component to a high-quality, fully accessible Angular Material component.\n\n## 2. Analysis & Problem Breakdown\n- Analyze the state management (e.g., hooks like `useState`, `useEffect`, `useContext`) and identify its reactive equivalent in Angular (Signals, RxJS, services).\n- Inspect the JSX structure and identify how to map the elements to Angular Material design primitives.\n- Review CSS/styles in the original file to ensure custom layouts are cleanly preserved or translated to Tailwind/SCSS.\n\n## 3. Pros & Cons of the Migration Target\n- **Pros:** Type-safety with Angular standalone components, excellent UX and accessibility out-of-the-box with Angular Material, cleaner change detection via Angular Signals.\n- **Cons:** Learning curve for React developers transitioning to Angular lifecycle/template syntax, slightly higher bundle size from Angular framework initialization.\n\n## 4. How to Work on It (Implementation Steps for Junior Devs)\n1. **Scaffold Component:** Run the scaffolding script or Angular CLI to generate the new standalone component structure.\n2. **Translate Templates:** Convert JSX elements to Angular HTML. Swap React elements for their appropriate `<mat-*>` directives.\n3. **Migrate State & Logic:** \n   - Replace local state (`useState`) with Angular `signal()`. \n   - Replace side-effects (`useEffect`) with Angular `effect()` or `ngOnInit` lifecycle hook.\n4. **Style Migration:** Adapt styles to `.scss`, prefixing or adjusting selectors to conform to Angular's view encapsulation.\n\n## 5. How to Reproduce & Local Verification\n- **Run Development Server:** Start the Angular local dev environment.\n- **Verify Behavior:** Build a simple demo route using the migrated component, passing dummy props.\n- **Check Layout & Interactivity:** Ensure state modifications correctly trigger UI updates and verify key accessibility indicators (ARIA attributes, keyboard navigation).\n\n## 6. Acceptance Criteria\n- [ ] Compiles successfully with no compiler errors or strict TypeScript warnings.\n- [ ] No React hook terminology or patterns remain in the TypeScript logic.\n- [ ] All JSX elements are completely mapped to Angular HTML syntax and Material elements.\n- [ ] Custom visual layouts are responsive and visual-parity with the original file is achieved.\n- [ ] Component is fully documented with basic inline documentation.\n\n## 7. Dependencies\n- {List dependencies or 'None'}"
  },
  {
    "title": "[Backend] Migrate {FileName} to Python",
    "labels": ["migration", "backend", "python"],
    "body": "## 1. Objective & Target\n- **Original File:** `{FilePath}`\n- **Target Tech Stack:** Python 3.10+, FastAPI, and Pydantic (v2)\n- **Goal:** Replicate the functionality of the Next.js API route into a clean, modern, and high-performance Python FastAPI backend endpoint.\n\n## 2. Analysis & Problem Breakdown\n- Analyze the request query parameters, body payloads, headers, and HTTP methods (GET, POST, etc.) in the original route.\n- Trace the database queries or external service invocations and plan equivalent Python SQLModel/SQLAlchemy operations.\n- Map the error response conditions and custom status codes in the Next.js API.\n\n## 3. Pros & Cons of the Migration Target\n- **Pros:** Blazing fast execution with FastAPI, automatic Swagger/OpenAPI documentation, strong type validation with Pydantic, and native asynchronous support.\n- **Cons:** Shifting from JavaScript/TypeScript ecosystem to Python, necessity of managing virtual environments, and database driver connection handling differences.\n\n## 4. How to Work on It (Implementation Steps for Junior Devs)\n1. **Generate Schema:** Convert TypeScript interfaces to Pydantic models for request validation and response serialization.\n2. **Set up Route Handlers:** Write a FastAPI route function with proper decorators (`@app.get()`, `@app.post()`, etc.) and type annotations.\n3. **Migrate Business Logic:** Re-implement business rules, validation logic, and data storage operations in idiomatic Python.\n4. **Error Handling:** Map original try/catch blocks to raise appropriate `HTTPException` exceptions with standardized error bodies.\n\n## 5. How to Reproduce & Local Verification\n- **Run Dev Server:** Start FastAPI with Uvicorn.\n- **Verify API Docs:** Open `/docs` to review auto-generated OpenAPI definitions.\n- **Test Request/Response Flow:** Use `curl` or Postman to submit valid and invalid payloads. Verify that response status codes and body structures match Next.js behaviors exactly.\n\n## 6. Acceptance Criteria\n- [ ] FastAPI endpoint is registered and passes all Python linter checks (Ruff, Flake8).\n- [ ] Input payload validation constraints (Pydantic schemas) are comprehensive and robust.\n- [ ] Endpoint responses match original TypeScript API interface shapes exactly.\n- [ ] Proper error handling returns structured error objects and correct HTTP status codes on failure.\n- [ ] The API successfully connects to mock/local databases or upstream services.\n\n## 7. Dependencies\n- {List dependencies or 'None'}"
  }
]
```
